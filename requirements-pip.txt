# Metixel Photoframe — Python Dependencies (Raspberry Pi, Trixie)
# Install with: pip install -r requirements-pip.txt
#
# Deliberately Qt-FREE.  PySide6 and python-mpv come from apt (see
# requirements-system.txt), for two reasons: pip's PySide6 wheel omits the GBM
# eglfs plugin, and this file drives the boot-time dependency self-heal in
# backend/dependencies.py, which runs as the hardened backend service and must
# not pull a GUI toolkit.

Pillow>=10.0
pillow-heif>=0.16
# NOTE: numpy is provided by apt (requirements-system.txt → python3-numpy) and
# Debian Trixie ships numpy 2.x.  Do NOT add an upper bound that excludes it
# (e.g. `<2.0`): pip would then try to DOWNGRADE numpy, fail to uninstall the
# apt-owned package (`no RECORD file`), and abort the whole install.  The code
# uses no numpy-2.0-removed APIs, and no dependency declares a numpy ceiling.
numpy>=1.24
Flask>=3.0
watchdog>=3.0
paho-mqtt>=1.6
requests>=2.31
cec>=0.2.8
